---
- name: Generate a Private Key
  openssl_privatekey:
    path: /etc/keycloak/certs/tls.key

- name: Generate CSR
  openssl_csr:
    path: /etc/keycloak/certs/tls.csr
    privatekey_path: /etc/keycloak/certs/tls.key
    digest: sha512
    common_name: "{{ inventory_hostname }}"

- name: Generate a Self Signed OpenSSL certificate
  openssl_certificate:
    path: /etc/keycloak/certs/tls.crt
    privatekey_path: /etc/keycloak/certs/tls.key
    csr_path: /etc/keycloak/certs/tls.csr
    provider: selfsigned

- name: copy cli files
  copy:
    src: cli/
    dest: "{{ keyclo_cli }}"
    mode: 0644
    owner: root
    directory_mode: yes

- name: copy conf files
  copy:
    src: conf/
    dest: "{{ keyclo_conf }}"
    mode: 0644
    owner: root
    directory_mode: yes

- name: copy docker files
  copy:
    src: "{{ item }}"
    dest: "{{ keyclo_dir }}"
    owner: root
    group: root
    mode: 0755
  with_items:
    - start.sh

